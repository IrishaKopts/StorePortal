import React from 'react';

export namespace LoginPage {
  export interface Props {
  }
}

export const LoginPage = ({ }: LoginPage.Props): JSX.Element => {

  return (
    <header>
      <h1>Login page</h1>
    </header>
  );
};

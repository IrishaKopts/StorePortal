import React from 'react';

export namespace HomePage {
  export interface Props {
  }
}

export const HomePage = ({ }: HomePage.Props): JSX.Element => {

  return (
    <header>
      <h1>Home page</h1>
    </header>
  );
};

import React from 'react';

export namespace WelcomePage {
  export interface Props {
  }
}

export const WelcomePage = ({ }: WelcomePage.Props): JSX.Element => {

  return (
    <header>
      <h1>Welcome page</h1>
    </header>
  );
};
